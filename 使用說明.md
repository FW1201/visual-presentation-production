# Visual Presentation Production 使用說明

## ChatGPT Work 安裝

1. 下載根目錄 `visual-presentation-production.zip`。
2. 在 ChatGPT 的個人頭像選單開啟 **Skills → New skill → Upload from your computer**，選取此 ZIP；不要把 ZIP 附在 Work 對話後要求 ChatGPT「安裝」。對話附件只會成為該次工作的參考資料，代理無權寫入你的個人 Skills 儲存區。
3. 等待掃描完成，並在 **Installed** 分頁確認 `Visual Presentation Production` 已出現，再建立或開啟 Work Project。
4. 在 Work Project 附加大綱、來源文件、品牌素材、參考投影片、logo 與必要圖表。若尚未有素材，請在需求中明確授權 Skill 提出內容架構。
5. 輸入：`使用 $visual-presentation-production，先逐步確認用途、內容架構、精確頁數、逐頁版面、風格與參考素材；完成後製作單一 PDF 簡報。`

若 Skills 頁面沒有 **Upload from your computer**、無法建立 skill，或上傳後沒有安裝按鈕，這是 Workspace 的角色權限，不是 ZIP 結構問題。請管理員在 Workspace 的 Skills 權限啟用 **Enable skills**、**Enable skill uploading** 與（需要代他人安裝時）**Enable skills installing**。若掃描顯示 **Needs Review** 或 **Blocked**，請依掃描結果處理；這與「無法寫入個人 Skills 儲存區」是不同問題。

Work 的最終交付固定為單一 PDF；不提供 PPTX、Google Slides、講者稿或 PNG 組。Work 中不可執行 `assemble_visual_deck.py`、安裝 Python 套件或寫入個人技能目錄。

## Codex 安裝

將解壓後的 `visual-presentation-production/` 放入 `~/.codex/skills/`，再以 `$visual-presentation-production` 啟動。

## 使用流程

Skill 會先逐關確認目的、受眾、內容來源／組織、精確投影片數、品牌與參考素材。接著產出與已確認頁數完全一致的 `outline.md`；每頁都含唯一主張、精確文字、素材、可直接執行的版面描述與風格規則。大綱、文字與樣張都核准後才逐張生成。

Codex 可用 `scripts/assemble_visual_deck.py` 將已核准的全頁 PNG 組成單一 `presentation.pdf` 與 `qa-report.json`。腳本會拒絕頁數不一致、缺少版面／風格規則、未核准大綱或樣張、未凍結文字及非 16:9 圖片；不繪圖、不補字，也不輸出 PPTX。

## 續作

保留 `deck-manifest.json` 與 `visual-style-profile.md`。沿用既有風格時仍需重新確認新簡報的目的、內容架構、精確頁數與素材，再生成並核准一張新樣張。
